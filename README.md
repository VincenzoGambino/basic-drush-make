# basic-drush-make
Basic drush make file

With this .make file you can create a drupal installation with the latest vesion of:
Drupal core, admin_menu, backup_migrate, context, ctools, date, devel, diff, entity, features, features_diff, features_extra, libraries, link, module_filter, pathauto, strongarm, token, views.
